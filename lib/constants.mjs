/**
 * Shared constants for rui CLI skills.
 * Imported by rui-bot, rui-npm, and other `.claude/rui-*` skills.
 */

/** Offset to skip `node <script>` from process.argv so args start at index 2. */
export const NODE_ARGV_OFFSET = 2;

/** Default max HTTP retries for notification senders. */
export const MAX_RETRIES = 3;
