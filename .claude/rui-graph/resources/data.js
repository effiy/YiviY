/* ════════════════════════════════════════════════════════════════════════
   rui-graph — Graph Data
   Generated from card analysis. window.GRAPH_DATA is read by index.js.

   Regenerate this file when card data changes.
   ════════════════════════════════════════════════════════════════════════ */

window.GRAPH_DATA = {
  elements: [
    // ── Card nodes ──
    // { data: { id: 'card:0', type: 'card', label: '🎥 yt-dlp', badge: 'OSS',
    //           desc: '...', tags: [...], meta: '...', richness: 5, href: '...' } }

    // ── Tag nodes ──
    // { data: { id: 'tag:Python', type: 'tag', label: 'Python', modifier: 'info', cooccurrence: 5 } }

    // ── Badge nodes ──
    // { data: { id: 'badge:OSS', type: 'badge', label: 'OSS', cardCount: 3 } }

    // ── Link Dest nodes ──
    // { data: { id: 'link_dest:github.com/yt-dlp/yt-dlp', type: 'link_dest',
    //           label: 'GitHub', url: 'https://github.com/yt-dlp/yt-dlp' } }

    // ── Edges ──
    // { data: { id: 'edge:card0:tag:Python', source: 'card:0', target: 'tag:Python', type: 'has_tag' } }
    // { data: { id: 'edge:card0:card1:shares_tag:Python', source: 'card:0', target: 'card:1',
    //           type: 'shares_tag', shared: 'Python' } }
  ],

  meta: {
    nodeCount: 0,
    edgeCount: 0,
    cardCount: 0,
    tagCount: 0,
    badges: [],
    source: '',
  }
};
