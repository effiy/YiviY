window.LIMITATIONS_DATA={en:{title:'Current Limitations',items:[{html:'WhisperX transcription performance can be affected by video background noise because it uses the wav2vec model for alignment. For videos with loud background music, enable vocal separation enhancement. Also, subtitles ending in digits or special characters may be truncated early because wav2vec cannot map digit characters (e.g. "1") to their spoken form ("one").'},{html:'Using weaker models may error mid-process due to strict JSON format requirements. If this happens, delete the <code>output</code> folder and retry with a different LLM — otherwise repeat execution will read the previous erroneous response and fail the same way.'},{html:'Due to differences in speech rate and intonation across languages, and the influence of the translation step, dubbing may not be 100% perfect. However, the project has done extensive engineering on speech rate to ensure the best dubbing result.'},{html:'<strong>Multilingual video transcription will only retain the main language.</strong> This is because whisperX uses language-specific models for forced alignment at word level, and will drop unrecognized languages.'},{html:'<strong>Cannot dub multiple speakers separately</strong>, as whisperX\'s speaker diarization is not reliable enough.'}]},'zh-CN':{title:'当前限制',items:[{html:'WhisperX 转录效果可能受到视频背景声影响，因为使用了 wav2vac 模型进行对齐。对于背景音乐较大的视频，请开启人声分离增强。另外，如果字幕以数字或特殊符号结尾，可能会导致提前截断，这是因为 wav2vac 无法将数字字符（如"1"）映射到其发音形式（"one"）。'},{html:'使用较弱模型时容易在中间过程报错，这是因为对响应的 json 格式要求较为严格。如果出现此错误，请删除 <code>output</code> 文件夹后更换 llm 重试，否则重复执行会读取上次错误的响应导致同样错误。'},{html:'配音功能由于不同语言的语速和语调差异，还受到翻译步骤的影响，可能不能 100% 完美，但本项目做了非常多的语速上的工程处理，尽可能保证配音效果。'},{html:'<strong>多语言视频转录识别仅仅只会保留主要语言</strong>，这是由于 whisperX 在强制对齐单词级字幕时使用的是针对单个语言的特化模型，会因为不认识另一种语言而删去。'},{html:'<strong>无法多角色分别配音</strong>，whisperX 的说话人区分效果不够好用。'}]}};